# Sub2API 完整多品牌架构设想（未来方案，当前未实施）

> 当前生产实现采用轻量域名配置，不新增品牌表、用户品牌字段或订单品牌字段。实际实现与维护规则以 `MULTI_BRAND_MINIMAL_PLAN_CN.md` 和 `skills/sub2api-domain-groups/SKILL.md` 为准；本文仅保留为需求升级后的架构参考。

## 1. 背景与目标

当前 `xiaohondou.com` 是 C 端主站，`xiaofanqie.org` 是独立分组的第二品牌，两者复用同一套 Sub2API 后端、数据库、Redis、用户体系、余额体系和上游账号池。`nideyiyi.com` 只迁移浏览器页面导航，`api.nideyiyi.com` 与所有旧 API 请求保留原 Host 直通，避免现有客户端被跨域永久重定向；两个旧域名的门户接口继承 `xiaohondou.com` 的分组范围，推理网关路径继续保持 Host 中立。

第一版的目标不是立即上线第二个品牌，而是先把系统改造成可扩展的品牌架构，同时保持默认单品牌运行：

- 默认品牌主域名为 `xiaohondou.com`，现有用户行为和价格不变。
- 默认关闭多品牌路由，不因请求 Host 不同改变现有行为。
- 品牌成为可配置的数据维度，后续增加 B 端品牌不需要复制后端或新建数据库。
- 分组归属品牌，为未来 B/C 两套独立定价提供可靠边界。
- 用户仍然全局唯一，只记录首次注册品牌，不在第一版中承担品牌权限作用。
- 尽量复用现有全局设置、注册流程和分组计费逻辑，避免改动网关、调度和核心计费链路。

## 2. 可行性与改动评估

方案可行，整体属于中等范围的横切改造，不是核心架构重写。

不需要改动的核心能力：

- API Key 鉴权和请求转发机制。
- 分组倍率计费逻辑和用量快照。
- 账号与分组的多对多绑定关系。
- Redis 调度快照和账号调度器。
- 用户余额、充值入账和并发控制。
- 管理员账号和后台总体权限模型。

需要改动的主要能力：

- 域名到品牌的解析和请求上下文。
- 公开站点配置及前端 HTML 缓存。
- 分组的品牌归属。
- 注册来源品牌及注册默认分组。
- 用户侧分组、套餐、API Key 等资源的品牌过滤和服务端校验。
- 支付订单、邮件和 OAuth 等异步流程中的品牌上下文保留。

第一版预计涉及数据库迁移、后端品牌基础设施、少量管理页面和回归测试。合理拆分后预计 5-7 个开发日；第二品牌正式上线前，再补齐 B 端准入和完整的品牌化支付/OAuth 验证，预计增加 4-6 个开发日。

## 3. 设计原则

### 3.1 默认单品牌兼容

增加系统设置 `multi_brand_enabled`，默认值为 `false`。

- 关闭时：所有请求使用默认品牌，保持现有 Host 兼容行为。
- 开启时：根据请求 Host 查找品牌，未知域名返回 `404`，不回退到默认品牌。
- 第一版生产发布后继续保持关闭，完成数据回填和兼容验证后再考虑新增品牌。

### 3.2 品牌通过请求上下文传递

新增 `BrandResolver` 中间件，将解析结果写入 `request.Context()`。业务 Service 使用统一的 `BrandFromContext(ctx)` 获取当前品牌，避免给大量现有方法增加 `brandID` 参数。

品牌来源只信任规范化后的 `Request.Host`：

- 转为小写。
- 去除端口和末尾点号。
- Nginx 必须使用 `proxy_set_header Host $host` 保留原始域名。
- 不直接信任客户端可伪造的 `X-Forwarded-Host`。

### 3.3 分组是定价隔离的唯一业务主线

每个分组只属于一个品牌。B/C 品牌分别建立独立分组记录，并配置各自倍率、套餐和默认注册分组。相同上游账号可以同时绑定两个品牌的分组，因此账号池可以共用，但价格独立。

第一版不在 API 网关数据面按域名限制 Key。API Key 继续通过其绑定分组决定调度和价格，避免破坏现有客户端域名和兼容地址。品牌隔离主要作用于门户、注册、资源展示、Key 创建和购买流程。

## 4. 数据模型

### 4.1 `brands`

新增品牌表：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | 主键 |
| `code` | varchar(50) | 稳定品牌编码，全局唯一 |
| `name` | varchar(100) | 管理后台显示名称 |
| `status` | varchar(20) | `active` / `inactive` |
| `is_default` | boolean | 是否默认品牌，全局只能有一个 |
| `config` | jsonb | 品牌公开配置覆盖项 |
| `created_at` / `updated_at` | timestamptz | 创建、更新时间 |

`config` 第一版只允许白名单字段：

- `site_name`
- `site_logo`
- `site_subtitle`
- `frontend_url`
- `api_base_url`
- `home_content`
- `contact_info`
- `doc_url`
- `theme`（受控 CSS 变量，不允许任意 CSS）

默认品牌的 `config` 可以为空，继续使用当前全局 Settings。新增品牌以全局设置为基础配置，再应用自己的覆盖项。这样无需迁移现有站点配置，也避免维护两套完整 Settings 结构。

### 4.2 `brand_domains`

新增品牌域名表：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | 主键 |
| `brand_id` | bigint | 所属品牌 |
| `hostname` | varchar(255) | 规范化域名，全局唯一 |
| `kind` | varchar(20) | `web` / `api` / `legacy` |
| `is_primary` | boolean | 是否品牌主域名 |
| `created_at` | timestamptz | 创建时间 |

默认品牌首批登记：

- `xiaohondou.com`（主域名）
- `xiaofanqie.org`（第二品牌域名）
- `nideyiyi.com`（浏览器页面临时迁移、API 原 Host 直通兼容域名）
- `api.nideyiyi.com`（原 Host 直通兼容 API 域名）

### 4.3 现有表字段

`groups` 增加：

- `brand_id bigint NOT NULL`。
- 现有分组全部回填为默认品牌。
- 将现有分组名称唯一索引从 `name` 调整为 `(brand_id, name)`，允许不同品牌使用相同分组名。
- 分组的 fallback、组合路由目标必须属于同一品牌；上游账号允许跨品牌复用。

`users` 增加：

- `signup_brand_id bigint NOT NULL`，现有用户回填默认品牌。
- `signup_host varchar(255)`，记录注册请求的实际域名。
- 仅在用户首次创建时写入，后续从其他品牌登录不修改。

`payment_orders` 增加：

- `brand_id bigint NOT NULL`，现有订单回填默认品牌。
- 用于异步支付回调、返回地址、订单查询和通知邮件恢复品牌上下文。

第一版不在 `api_keys` 和 `usage_logs` 重复存储 `brand_id`，品牌通过 `group_id` 派生，避免数据漂移。历史无分组 Key 视为默认品牌资源。

## 5. 品牌配置解析

新增 `BrandConfigResolver`：

1. 读取现有公共 Settings，得到基础配置。
2. 从请求上下文读取当前品牌。
3. 默认品牌直接使用基础配置。
4. 非默认品牌使用 `brands.config` 覆盖白名单字段。
5. 返回统一的 `PublicSettings`，前端无需维护两套接口。

现有 `/api/v1/settings/public` 和服务端 `window.__APP_CONFIG__` 注入均改为调用该解析器。

当前前端 HTML 只使用一个进程内缓存。多品牌模式下必须改成按 `brand_id` 缓存：

- 缓存键：`brand:<id>`。
- 每个品牌独立 ETag。
- 更新全局公共设置时清空全部品牌缓存。
- 更新单个品牌配置时只清空该品牌缓存。
- 增加 A/B Host 交替访问测试，防止 Logo、站点名和配置串站。

## 6. 业务改造范围

### 6.1 注册与登录

- 邮箱注册、OAuth 新建用户和待完成 OAuth 会话都保存当前 `brand_id`。
- 注册流程保持上游行为，不由品牌配置自动创建 API Key。
- 登录只验证全局用户，不限制注册品牌，也不修改 `signup_brand_id`。
- 用户表邮箱唯一约束保持不变，同一个邮箱不会因品牌产生两条用户记录。
- 两个不同顶级域名的浏览器登录状态默认不共享；用户可以用同一账号分别登录。

### 6.2 分组与 API Key

以下用户侧操作必须限制到当前品牌：

- 获取可用分组及用户专属倍率。
- 创建 API Key 和修改 API Key 分组。
- API Key 列表、详情、删除和筛选。
- 可用渠道及其他按分组展示的门户页面。

所有写操作必须在 Service 层调用统一校验：

```text
RequireGroupInCurrentBrand(ctx, groupID)
```

不能只依赖前端隐藏选项。跨品牌构造 `group_id` 时统一返回资源不存在，避免暴露另一品牌信息。

### 6.3 套餐与支付

- 套餐品牌由其 `group_id` 派生。
- 套餐列表和 checkout-info 只返回当前品牌分组的套餐。
- 创建订单时重新校验套餐分组属于当前品牌，阻止构造其他品牌 `plan_id`。
- 订单保存 `brand_id`，支付恢复页、异步通知和成功邮件使用订单品牌。
- 用户余额第一版继续全品牌共享。
- 支付方式、充值倍率和充值上下限第一版继续使用全局设置；后续确有 B/C 差异时再加入品牌覆盖字段。

### 6.4 兑换码、订阅与用量

- 绑定分组或套餐的兑换码必须校验当前品牌。
- 用户订阅列表按分组品牌过滤。
- 用户侧用量记录和分组筛选按当前品牌过滤。
- 管理后台仍可跨品牌查看全部记录，并增加品牌筛选条件。

### 6.5 邮件与 OAuth

- 注册验证码、密码重置、支付成功等用户触发邮件携带品牌上下文。
- 异步任务不能依赖当前 HTTP Context，必须持久化 `brand_id` 或在任务载荷中保存。
- 密码重置链接使用发起请求品牌的 `frontend_url`。
- OAuth state 或 pending session 保存 `brand_id`，回调完成后恢复原品牌。
- 第二品牌上线前，需要在对应 OAuth 平台登记新的回调域名。

## 7. 管理后台方案

第一版增加独立的“品牌管理”页面，避免大范围改造现有系统设置页面：

- 品牌列表：名称、编码、状态、默认品牌、主域名。
- 品牌编辑：域名、公开配置覆盖、主题变量、默认注册分组。
- 默认品牌的站点内容仍从现有“系统设置”维护；品牌页面只展示其继承关系和域名。
- 分组新增/编辑页增加品牌选择器。
- 系统只有一个品牌时自动选中默认品牌，并隐藏不必要的品牌选择控件。
- 用户列表增加“注册品牌”列和筛选项，仅作为来源标记。

管理员接口不默认按当前 Host 隔离，管理员可以统一管理所有品牌。所有管理写操作记录品牌 ID 到审计日志。

## 8. 前端样式策略

继续使用同一套 Vue 构建产物，不复制前端项目。

品牌配置通过 `PublicSettings` 注入并驱动：

- 站点名称、Logo、favicon 和页面标题。
- 首页内容、联系信息、文档地址和自定义菜单。
- 受控主题变量，例如主色、强调色、侧栏色和按钮色。
- 后续可以增加品牌功能开关，但第一版不允许任意脚本或 CSS 注入。

如果未来 B 端产品流程与 C 端出现结构性差异，再在同一前端内通过品牌功能开关选择页面模块，不在第一版拆成两个前端仓库。

## 9. 商业与容量边界

域名隔离本身不是价格权限控制。第二品牌正式上线时，如果 B 端价格低于 C 端，公开登录会允许 C 端用户访问 B 端域名并使用同一账号创建 B 端 Key。

B 端上线前建议选择一种准入方式：

- 关闭公开注册，只允许管理员分配。
- 使用 B 端邀请码。
- 增加 `user_brand_memberships`，支持 `pending` / `active` / `disabled` 的企业客户审批。

第一版只预留品牌维度，不增加成员表，避免当前单品牌阶段扩大改动。

另外，共用上游账号池只能实现价格隔离，不能实现容量或 SLA 隔离。B 端需要独立服务保障时，可以继续复用同一后端，但应给 B 端分组配置独立账号池或明确的容量配额。

## 10. 分阶段实施

### 阶段一：兼容基础设施

- 新增 `brands`、`brand_domains` 和相关字段。
- 创建默认品牌并完成数据回填。
- 增加 `BrandResolver`、`BrandContext` 和 `BrandConfigResolver`。
- 增加 `multi_brand_enabled=false`。
- 调整分组唯一索引。
- 完成数据库迁移和回滚校验。

### 阶段二：默认品牌全链路接入

- 公共设置、HTML 注入和缓存接入品牌上下文。
- 注册来源可在完整品牌模型阶段按业务需要接入品牌。
- 用户侧分组、Key、套餐、订阅、用量和兑换码接入品牌过滤。
- 订单持久化品牌并补充服务端越权校验。
- 默认关闭多品牌，验证现网行为保持不变。

### 阶段三：管理和前端配置

- 增加品牌管理 API 和页面。
- 分组页增加品牌归属。
- 用户页增加注册品牌标记和筛选。
- 前端接入主题变量和品牌内容覆盖。

### 阶段四：第二品牌上线

- 创建 B 端品牌、域名和分组。
- 将需要共用的上游账号绑定到 B 端分组。
- 配置 B 端默认分组、样式、套餐和文档。
- 完成 DNS、TLS、Cloudflare 和 Nginx 配置。
- 配置 B 端准入策略。
- 开启 `multi_brand_enabled` 并灰度验证。

## 11. 验收标准

### 11.1 第一版单品牌兼容验收

- `multi_brand_enabled=false` 时，现有域名页面和 API 行为与改造前一致。
- 所有现有分组、用户和订单均归属默认品牌，无空品牌记录。
- 注册流程不自动创建默认 API Key。
- 现有用户余额、Key、订阅、用量和支付订单可正常读取。
- API 网关、模型列表、请求调度和计费结果无回归。
- 全量 Go 测试、前端测试、类型检查、lint 和生产构建通过。

### 11.2 多品牌能力验收

- Host A/B 分别返回正确的站点名、Logo、主题和链接。
- A/B 交替访问不会复用对方 HTML 缓存或 ETag。
- A 域名只能看到和选择 A 品牌分组，B 域名同理。
- 构造跨品牌 `group_id`、`plan_id` 或兑换码会被服务端拒绝。
- 同一邮箱在两个品牌登录仍对应同一用户 ID。
- `signup_brand_id` 仅在首次注册时写入，跨品牌登录不会改变。
- 两个品牌分组可绑定相同上游账号，但分别使用各自倍率计费。
- 未登记 Host 在多品牌开启后返回 `404`。

## 12. 风险与控制

| 风险 | 控制措施 |
| --- | --- |
| 品牌页面配置串站 | HTML/PublicSettings 缓存按品牌键隔离，并增加交替 Host 测试 |
| 前端隐藏但接口可越权 | 在 Service 层统一校验分组和套餐品牌 |
| 默认品牌上线后行为变化 | 多品牌开关默认关闭，现有 Settings 作为默认品牌基线 |
| OAuth 回调丢失品牌 | state/pending session 持久化品牌 ID |
| 异步支付或邮件使用错误域名 | 订单或任务载荷持久化品牌 ID |
| 分组 fallback 跨品牌 | 分组保存时校验所有关联分组品牌一致 |
| 升级后浏览器 API 预检 `403` | 保留运行时 `cors.allowed_origins` 多域名白名单，并以真实 `OPTIONS` 请求验收，不能只看 `/health` |
| B 端低价被公开用户使用 | B 端上线前增加邀请码、审批或成员资格 |
| 共用账号池互相抢占容量 | 需要 SLA 时拆账号绑定或增加容量配额 |

## 13. 最终建议

第一版采用“默认品牌 + 品牌覆盖配置”的方式，不迁移现有全局 Settings，不拆后端，不改网关数据面，也不提前引入 B 端成员权限。当前轻量实现以 `xiaohondou.com` 作为默认品牌主域名，旧 `nideyiyi.com` 仅迁移浏览器页面，API 与 `api.nideyiyi.com` 保留原 Host 直通兼容。

这个范围能控制改动量，同时避免把多品牌做成仅靠前端判断或域名配置的临时方案。第二品牌上线时主要是新增品牌数据、B 端分组和准入策略，而不是再次重构现有注册、计费和门户链路。
